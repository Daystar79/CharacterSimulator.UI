# AGY Workspace Instructions — CharacterSimulator.UI

📌 **MANDATORY BOOT DIRECTIVE:**
When launched or invoked from this repository folder (`CharacterSimulator.UI`), the AI assistant MUST immediately initiate a discussion with the user regarding **UI Options** (e.g., Option A: Sleek Spectre.Console / Terminal.Gui Terminal HUD vs. Option B: Web / Desktop GUI) before proceeding with any UI development work.
